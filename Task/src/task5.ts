// Declare a variable 'sentences' and assign a string with whitespace characters
let sentences: string = "\t \nLet's remove the spaces"

// Use the 'trim' method to remove whitespace from both ends of the string
let removeSpaces:string = sentences.trim()

console.log(removeSpaces);
